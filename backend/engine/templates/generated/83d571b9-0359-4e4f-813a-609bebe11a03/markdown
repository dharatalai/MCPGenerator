# DeepSearch MCP Server

This is a Model Context Protocol (MCP) server implementation for the DeepSearch AI service.

## Features

- Implements the DeepSearch chat completions API
- Supports streaming responses
- Comprehensive error handling
- Rate limiting (10 requests per minute)
- Type-safe with Pydantic models

## Installation

1. Clone this repository
2. Create a virtual environment: `python -m venv venv`
3. Activate the virtual environment:
   - On Windows: `venv\Scripts\activate`
   - On Unix/MacOS: `source venv/bin/activate`
4. Install dependencies: `pip install -r requirements.txt`
5. Create a `.env` file based on `.env.example` and add your API key

## Running the Server

Start the server with: